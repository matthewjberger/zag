/target

# Generated. `just port <name>` writes one from the tables in zag-facts, which
# is what the tree carries instead.
*.facts

*.tmp
*.log

# Zig build output inside the example projects.
.zig-cache/
zig-out/
corpus/*/zig-out/
corpus/*/.zig-cache/
