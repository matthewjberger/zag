zag ownership report
target: x86_64-linux
fields: 9

Buffer.data
  class: owned
  confidence: high
  evidence: freed inside the deinit call closure (release)
  evidence: assigned from an allocation (init)
  evidence: allocator resolves to the global allocator

Buffer.length
  class: value
  confidence: high

Header.magic
  class: value
  confidence: high

Header.version
  class: value
  confidence: high

Header.flags
  class: value
  confidence: high

Node.label
  class: arena
  confidence: high
  evidence: assigned from an allocation (parseNode)
  evidence: allocator resolves to an arena

Node.children
  class: static
  confidence: high
  evidence: assigned from a static literal (parseNode)

View.bytes
  class: borrowed
  confidence: medium
  evidence: assigned from a parameter the caller retains (makeView)

Cache.entries
  class: unknown
  confidence: low
  evidence: no assignment to this field was found

functions: 7
  Buffer.init: still to write, the error set it can fail with has no name (example.zig:16)
    no constructor: nothing the port could read assigns data
  Buffer.deinit: disappears, Drop frees what it freed (example.zig:23)
  release: disappears, Drop frees what it freed (example.zig:28)
  makeBuffer: still to write, the error set it can fail with has no name (example.zig:33)
  parseNode: still to write, the error set it can fail with has no name (example.zig:48)
  parseTree: still to write, the error set it can fail with has no name (example.zig:57)
  makeView: ported, signature only, the body is still to write (example.zig:66)
